<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, as: :string, label: "Pose une question", required: true, placeholder: "Par où dois-je commencer" %>
  <%= f.button :submit, 'Go Muscu', class: 'btn btn-primary' %>
<% end %>
